int[] firstReverseTry(int[] arr) {
    
    int len=arr.length;
    
    if(len > 0){
        int temp=arr[0];
        arr[0]=arr[len-1];
        arr[len-1]=temp;
    }
    
    
    
    return arr;
    
    
}
